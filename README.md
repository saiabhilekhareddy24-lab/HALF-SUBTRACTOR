# HALF-SUBTRACTOR
Half Subtractor is a combinational digital circuit used to subtract one 1-bit binary number from another. It produces two outputs: Difference and Borrow, using XOR, NOT, and AND logic operations.
